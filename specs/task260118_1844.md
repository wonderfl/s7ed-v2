# 작업계획서: tab_drawers.py 분리 작업

**작업 일시**: 2026-01-18 18:44  
**작업 목표**: `tab_drawers.py`에서 전체탭(`_draw_all_tab_polygons`)과 나머지 탭 메서드들을 별도 파일로 분리

## 현재 구조 분석

### 파일 구조
- `gui/face_edit/polygon_renderer/tab_drawers.py` (1961줄)
  - `TabDrawersMixin` 클래스 하나에 7개 메서드 포함:
    1. `_draw_all_tab_polygons` (11~1056줄, 약 1046줄) - 전체탭
    2. `_draw_eye_tab_polygons` (1057~1307줄)
    3. `_draw_nose_tab_polygons` (1308~1417줄)
    4. `_draw_mouth_tab_polygons` (1418~1532줄)
    5. `_draw_eyebrow_tab_polygons` (1533~1700줄)
    6. `_draw_jaw_tab_polygons` (1701~1862줄)
    7. `_draw_contour_tab_polygons` (1863~1961줄)

### 의존성
- `gui/face_edit/polygon_renderer/drawing.py`에서 `TabDrawersMixin`을 import하여 상속

## 작업 계획

### 단계 1: 전체탭 전용 파일 생성
- [x] `gui/face_edit/polygon_renderer/all_tab_drawer.py` 파일 생성
- [x] `AllTabDrawerMixin` 클래스 생성
- [x] `_draw_all_tab_polygons` 메서드만 이동 (11~1056줄)
- [x] 필요한 import 문 복사

### 단계 2: 기존 tab_drawers.py 수정
- [x] `_draw_all_tab_polygons` 메서드 제거
- [x] 나머지 6개 메서드만 유지
- [x] 클래스명은 `TabDrawersMixin` 유지 (하위 호환성)

### 단계 3: drawing.py 수정
- [x] `all_tab_drawer.py`에서 `AllTabDrawerMixin` import 추가
- [x] `DrawingMixin`이 두 Mixin을 모두 상속받도록 수정:
  ```python
  class DrawingMixin(AllTabDrawerMixin, TabDrawersMixin):
  ```

### 단계 4: 검증
- [x] import 오류 확인
- [x] 메서드 호출 경로 확인 (tab_renderer.py 등에서 사용하는지 확인)
- [x] 린터 오류 확인

## 예상 결과

### 생성될 파일
- `gui/face_edit/polygon_renderer/all_tab_drawer.py` (약 1046줄)

### 수정될 파일
- `gui/face_edit/polygon_renderer/tab_drawers.py` (약 905줄로 축소)
- `gui/face_edit/polygon_renderer/drawing.py` (import 및 상속 수정)

## 주의사항
1. 메서드 시그니처 변경 없음 (하위 호환성 유지)
2. 공통 헬퍼 메서드가 있다면 확인 필요
3. import 순서 주의 (MRO 고려)
