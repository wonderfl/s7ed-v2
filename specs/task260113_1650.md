# 작업 계획서: frame 모듈 폴더 구조 정리

**작업 일시**: 2026-01-13 16:50  
**작업 목표**: `gui/frame*.py` 파일들을 `gui/frame/` 폴더로 이동하여 일관된 구조로 정리

## 작업 배경
- 현재 `gui/` 폴더에 `frame*.py` 파일들이 흩어져 있음
- `face_edit`, `face_extract` 모듈은 이미 폴더 구조로 정리되어 있음
- 일관된 구조를 위해 `frame`도 동일한 패턴으로 정리 필요
- 코드 가독성 및 유지보수성 향상

## 현재 상태 분석

### 이동 대상 파일 목록
1. `gui/frame_basic.py` → `gui/frame/basic.py`
2. `gui/frame_button.py` → `gui/frame/button.py`
3. `gui/frame_equip.py` → `gui/frame/equip.py`
4. `gui/frame_listup.py` → `gui/frame/listup.py`
5. `gui/frame_personality.py` → `gui/frame/personality.py`
6. `gui/frame_skill.py` → `gui/frame/skill.py`

### 참조하는 파일 목록
1. `gui/gui.py`: `import gui.frame_button as _button`
2. `gui/_general.py`: 
   - `from . import frame_listup as _listup`
   - `from . import frame_basic as _basic`
   - `from . import frame_skill as _skill`
   - `from . import frame_equip as _equip`
   - `from . import frame_personality as _personality`
   - `from . import frame_button as _button`
3. `gui/face_extract/__init__.py`: `import gui.frame_basic as _basic`
4. `gui/_face_import.py`: `import gui.frame_basic as _basic`

## 작업 순서

### 1단계: 폴더 구조 생성
- [x] `gui/frame/` 폴더 생성
- [x] 폴더 구조 확인

### 2단계: 파일 이동 및 이름 변경
- [x] `frame_basic.py` → `frame/basic.py` 이동 및 이름 변경
- [x] `frame_button.py` → `frame/button.py` 이동 및 이름 변경
- [x] `frame_equip.py` → `frame/equip.py` 이동 및 이름 변경
- [x] `frame_listup.py` → `frame/listup.py` 이동 및 이름 변경
- [x] `frame_personality.py` → `frame/personality.py` 이동 및 이름 변경
- [x] `frame_skill.py` → `frame/skill.py` 이동 및 이름 변경

### 3단계: 패키지 초기화 파일 생성
- [x] `gui/frame/__init__.py` 생성 완료

### 4단계: 외부 참조 파일 import 경로 수정
- [x] `gui/gui.py` 수정
  - `import gui.frame_button as _button` → `from gui.frame import button as _button`
- [x] `gui/_general.py` 수정
  - `from . import frame_*` → `from .frame import *` (6개 모듈 모두 수정)
- [x] `gui/face_extract/__init__.py` 수정
  - `import gui.frame_basic as _basic` → `from gui.frame import basic as _basic`
- [x] `gui/_face_import.py` 수정
  - `import gui.frame_basic as _basic` → `from gui.frame import basic as _basic`

### 5단계: 기타 참조 확인
- [x] 프로젝트 전체에서 `frame_*` 관련 import 검색
- [x] 누락된 참조가 있는지 확인 및 수정 (모든 import 경로 수정 완료)

### 6단계: 테스트 및 검증
- [x] import 오류 확인 (린터 오류 없음)
- [ ] 프로그램 실행 테스트 (사용자 확인 필요)
- [ ] frame 관련 기능 동작 확인 (사용자 확인 필요)

### 7단계: 기존 파일 삭제
- [x] 모든 테스트 완료 후 기존 `frame*.py` 파일 삭제 확인 (모든 파일 이동 완료, 기존 파일 없음)
- [x] Git 상태 확인

## 기술 사항
- Python 패키지 구조: `gui/frame/` 폴더에 `__init__.py` 포함 (필요시)
- 절대 import 사용: `from gui.frame.module import` 형식
- 상대 import 사용: `from .frame.module import` 형식
- face_edit, face_extract 모듈과 동일한 구조 패턴 적용

## 최종 구조

```
gui/
├── frame/
│   ├── __init__.py          (필요시)
│   ├── basic.py
│   ├── button.py
│   ├── equip.py
│   ├── listup.py
│   ├── personality.py
│   └── skill.py
├── face_extract/
│   └── ...
└── face_edit/
    └── ...
```

## 주의 사항
- 파일 이동 시 Git 히스토리 유지를 위해 `git mv` 사용 고려
- import 경로 수정 시 오타 주의
- 모든 참조를 찾아 수정해야 함
- 테스트 완료 전까지 기존 파일 삭제 금지
- 린터 오류 확인 필수
